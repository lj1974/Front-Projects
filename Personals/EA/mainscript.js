const swiper = new Swiper('.swiper-container', {
  loop: true,
  slidesPerView: 'auto',
  spaceBetween: 4,
  // If we need pagination
  pagination: {
    el: '.swiper-pagination',
    dynamicBullets: true,
    cicklabe: true
  }
})
